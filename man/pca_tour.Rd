% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tour.R
\name{pca_tour}
\alias{pca_tour}
\title{Explore high-dimensional data with projection tours anchored to PCA}
\usage{
pca_tour(
  fit,
  type = c("grand", "guided", "local", "radial", "history"),
  dims = c(1, 2),
  group = NULL,
  angle = pi/8,
  variable = 1L,
  max_frames = 60L,
  save_history = FALSE,
  gif_file = NULL,
  ...
)
}
\arguments{
\item{fit}{A \code{pca_fit} object with explicit loadings.}

\item{type}{\code{"grand"}, \code{"guided"}, \code{"local"}, \code{"radial"}, or \code{"history"}.}

\item{dims}{Starting PCA dimensions, usually c(1,2).}

\item{group}{Optional point grouping for display.}

\item{angle}{Local-tour angular radius.}

\item{variable}{Variable index used by radial tour.}

\item{max_frames}{Maximum interactive frames.}

\item{save_history}{Return a saved basis history instead of animating.}

\item{gif_file}{Optional GIF destination; when supplied, render the saved tour history to a GIF.}

\item{...}{Additional arguments passed to tourr animation/history functions.}
}
\description{
Explore high-dimensional data with projection tours anchored to PCA
}
\examples{
if (requireNamespace("tourr", quietly = TRUE)) {
  fit <- pca_fit(iris[, 1:4], center = TRUE, scale = TRUE, ncomp = 4)
  # headless-safe form: save projection histories instead of animating
  h <- pca_tour(fit, type = "grand", max_frames = 20, save_history = TRUE)
  length(h)
  h <- pca_tour(fit, type = "local", max_frames = 20, save_history = TRUE)
  length(h)
  \dontrun{
    pca_tour(fit, type = "guided")  # opens an interactive animation
  }
}
}
