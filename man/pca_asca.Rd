% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/structured.R
\name{pca_asca}
\alias{pca_asca}
\title{ANOVA-Simultaneous Component Analysis (ASCA)}
\usage{
pca_asca(
  data,
  formula,
  design,
  ncomp = 2L,
  scale = FALSE,
  nperm = 0L,
  seed = NULL
)
}
\arguments{
\item{data}{Numeric response matrix (observations x variables).}

\item{formula}{Model formula using columns in \code{design}.}

\item{design}{Data frame containing experimental factors/covariates.}

\item{ncomp}{Components retained per effect.}

\item{scale}{Scale response variables before decomposition.}

\item{nperm}{Number of Freedman-Lane residual permutations for term-level sums of squares; 0 disables testing.}

\item{seed}{Optional random seed for permutations.}
}
\value{
A list containing effect matrices, PCA fits, sums of squares, and design information.
}
\description{
Decomposes a multivariate response matrix into model-effect matrices using
linear-model projections, then applies PCA to each effect matrix.
}
\examples{
set.seed(1)
design <- data.frame(A = factor(rep(1:2, each = 10)),
                     B = factor(rep(1:2, 10)))
Y <- cbind(y1 = rnorm(20) + 2 * as.numeric(design$A),
           y2 = rnorm(20) + as.numeric(design$B))
asca <- pca_asca(Y, ~ A + B, design, ncomp = 1, nperm = 19, seed = 1)
asca$effect_ss
asca$effect_p
}
