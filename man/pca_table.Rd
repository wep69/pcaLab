% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/reporting.R
\name{pca_table}
\alias{pca_table}
\title{Create publication-ready PCA tables}
\usage{
pca_table(
  fit,
  type = c("eigenvalues", "loadings", "scores", "variables", "diagnostics",
    "preprocessing", "ncomp", "inference_global", "inference_axes",
    "bootstrap_eigenvalues", "associations"),
  component = 1L,
  selection = NULL,
  inference = NULL,
  bootstrap = NULL,
  association = NULL,
  format = c("data.frame", "gt")
)
}
\arguments{
\item{fit}{A \code{pca_fit} object.}

\item{type}{\code{"eigenvalues"}, \code{"loadings"}, \code{"scores"}, \code{"variables"},
\code{"diagnostics"}, \code{"preprocessing"}, \code{"ncomp"}, \code{"inference_global"},
\code{"inference_axes"}, \code{"bootstrap_eigenvalues"}, or \code{"associations"}.}

\item{component}{Optional component for variable summaries.}

\item{selection}{Optional result from \code{pca_ncomp()} used by \code{type = "ncomp"}.}

\item{inference}{Optional result from \code{pca_test()}.}

\item{bootstrap}{Optional result from \code{pca_boot()}.}

\item{association}{Optional result from \code{pca_associate()}.}

\item{format}{\code{"data.frame"} or \code{"gt"}.}
}
\value{
A data frame or gt table.
}
\description{
Create publication-ready PCA tables
}
\examples{
fit <- pca_fit(iris[, 1:4], center = TRUE, scale = TRUE, ncomp = 3)
pca_table(fit, type = "eigenvalues")
pca_table(fit, type = "variables", component = 1)
}
