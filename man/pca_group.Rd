% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/groups.R
\name{pca_group}
\alias{pca_group}
\title{Add external group summaries and confidence regions to PCA scores}
\usage{
pca_group(
  fit,
  group,
  dims = c(1, 2),
  region = c("mean_chisq", "data_chisq", "bootstrap", "circle_chisq", "none"),
  level = 0.95,
  nboot = 999L,
  npoints = 200L,
  seed = NULL
)
}
\arguments{
\item{fit}{A \code{pca_fit} object.}

\item{group}{Group/treatment label with one value per fitted observation.}

\item{dims}{Two principal-component indices.}

\item{region}{\code{"mean_chisq"}, \code{"data_chisq"}, \code{"bootstrap"}, \code{"circle_chisq"}, or \code{"none"}.}

\item{level}{Confidence/content level.}

\item{nboot}{Bootstrap replicates for centroid regions.}

\item{npoints}{Number of points per region curve.}

\item{seed}{Optional seed.}
}
\value{
Group centroids, sizes, and region coordinates.
}
\description{
PCA itself remains unsupervised; group labels are used only after fitting.
}
\examples{
fit <- pca_fit(iris[, 1:4], center = TRUE, scale = TRUE, ncomp = 3)
grp <- pca_group(fit, iris$Species, region = "data_chisq")
grp$centroids
head(grp$regions)
# group labels remain supplementary to the unsupervised fit
}
