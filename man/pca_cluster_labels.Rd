% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/clustering.R
\name{pca_cluster_labels}
\alias{pca_cluster_labels}
\title{Generate observation labels for PCA score plots}
\usage{
pca_cluster_labels(
  x,
  clustering,
  type = c("observation", "mean", "group", "external"),
  labels = NULL,
  dims = NULL
)
}
\arguments{
\item{x}{A \code{pca_fit} object or numeric matrix.}

\item{clustering}{Integer cluster membership vector.}

\item{type}{Label type: \code{"observation"}, \code{"mean"}, \code{"group"},
or \code{"external"}.}

\item{labels}{Character vector of custom labels (only used when
\code{type = "external"}).}

\item{dims}{Integer vector of score/component indices used.}
}
\value{
A character vector of labels.

A character vector of labels with one entry per observation.
}
\description{
Creates labels for observations based on cluster assignments.

Creates labels for observations based on cluster assignments,
either from observation names, cluster means, group titles, or a custom
external list.
}
\examples{
cl <- pca_cluster(iris[, 1:4], method = "kmeans", k = 3)
pca_cluster_labels(iris[, 1:4], cl$cluster, type = "group")
pca_cluster_labels(iris[, 1:4], cl$cluster, type = "mean")
}
