% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/clustering.R
\name{pca_cluster_boundary}
\alias{pca_cluster_boundary}
\title{Compute cluster boundaries for PCA score plots}
\usage{
pca_cluster_boundary(
  x,
  clustering,
  method = c("ellipse", "convex", "svm"),
  level = 0.95,
  dims = 1:2,
  npoints = 200
)
}
\arguments{
\item{x}{A \code{pca_fit} object or numeric matrix.}

\item{clustering}{Integer cluster membership vector.}

\item{method}{Boundary method: \code{"ellipse"}, \code{"convex"}, or
\code{"svm"}.}

\item{level}{Confidence level for Mahalanobis ellipses (default 0.95).}

\item{dims}{Integer vector of score/component indices (default 1:2).}

\item{npoints}{Number of points per ellipse curve.}
}
\value{
A \code{pca_cluster_boundary} object.

A \code{pca_cluster_boundary} object with boundary data.
}
\description{
Computes visual cluster boundaries using Mahalanobis ellipses,
convex hulls, or SVM decision surfaces.

Computes visual cluster boundaries using Mahalanobis ellipses,
convex hulls, or SVM decision surfaces.
}
\examples{
cl <- pca_cluster(iris[, 1:4], method = "kmeans", k = 3)
bd <- pca_cluster_boundary(iris[, 1:4], cl$cluster, method = "ellipse")
nrow(bd$boundaries)
}
