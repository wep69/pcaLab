% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/clustering.R
\name{pca_cluster_compare}
\alias{pca_cluster_compare}
\title{Compare clustering methods on PCA scores}
\usage{
pca_cluster_compare(
  x,
  methods = c("kmeans", "pam", "hclust"),
  k = NULL,
  dims = NULL,
  nstart = 25,
  indices = c("silhouette", "ch", "wss"),
  ...
)
}
\arguments{
\item{x}{A \code{pca_fit} object or numeric matrix.}

\item{methods}{Character vector of methods to compare.}

\item{k}{Number of clusters (recycled to all methods).}

\item{dims}{Integer vector of score/component indices.}

\item{nstart}{Number of random starts for k-means.}

\item{indices}{Character vector of validation indices:
\code{"silhouette"}, \code{"ch"}, \code{"dunn"}, \code{"wss"}, \code{"BIC"}.}

\item{...}{Additional arguments passed to methods.}
}
\value{
A \code{pca_cluster_compare} object.

A \code{pca_cluster_compare} object with comparison table.
}
\description{
Runs multiple clustering methods and returns a comparison table
of internal validation indices plus adjusted Rand index between pairs.

Runs multiple clustering methods and returns a comparison table
of internal validation indices.
}
\examples{
cmp <- pca_cluster_compare(iris[, 1:4], methods = c("kmeans", "pam", "hclust"), k = 3)
cmp
}
