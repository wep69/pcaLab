% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plots.R
\name{pca_plot}
\alias{pca_plot}
\title{Publication-ready PCA figures}
\usage{
pca_plot(
  fit,
  type = c("scree", "cumulative", "scores", "loadings", "correlation_circle",
    "contributions", "cos2", "biplot", "diagnostics", "bootstrap_loadings", "eigen_ci",
    "inference_axes", "dimension_selection", "trajectory", "eigenfunctions",
    "reconstruction", "scores3d"),
  dims = c(1, 2),
  component = 1L,
  group = NULL,
  labels = FALSE,
  group_region = NULL,
  bootstrap = NULL,
  selection = NULL,
  inference = NULL,
  base_size = 11
)
}
\arguments{
\item{fit}{A \code{pca_fit} object.}

\item{type}{Figure type: scree, cumulative, scores, loadings, correlation_circle,
contributions, cos2, biplot, diagnostics, bootstrap_loadings, eigen_ci, inference_axes, dimension_selection, trajectory, eigenfunctions, reconstruction, or scores3d.}

\item{dims}{Component indices used by two-dimensional figures.}

\item{component}{Component used by one-dimensional variable summaries.}

\item{group}{Optional supplementary group labels for score plots.}

\item{labels}{Label observations/variables where meaningful.}

\item{group_region}{Optional result from \code{pca_group()}.}

\item{bootstrap}{Optional result from \code{pca_boot()} for bootstrap-based plots.}

\item{selection}{Optional result from \code{pca_ncomp()} for dimensionality-selection plots.}

\item{inference}{Optional result from \code{pca_test()} for inferential plots.}

\item{base_size}{Publication-theme base font size.}
}
\value{
A ggplot object.
}
\description{
Publication-ready PCA figures
}
\examples{
if (requireNamespace("ggplot2", quietly = TRUE)) {
  fit <- pca_fit(iris[, 1:4], center = TRUE, scale = TRUE, ncomp = 3)
  pca_plot(fit, type = "scree")
  pca_plot(fit, type = "scores", group = iris$Species)
  pca_plot(fit, type = "loadings", component = 1)
  pca_plot(fit, type = "correlation_circle")
}
}
